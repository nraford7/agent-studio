# {{name}}

> {{type}} — {{purpose in one line}}. Created {{created}}. Kernel {{kernel}},
> overlays: {{overlays}}.

## Purpose

{{One paragraph: what this team is for and which durable output it is —
reusable staffed workflow or standing team.}}

## The team

- {{For each in-house job: job name — the regular agent / tool / playbook that
  covers it.}}
- {{For each specialist seat: name (role) — what this seat is for; one line. Note
  the evidence conclusion that opened it.}}

## Charter record

{{Organization approved with the user on {{date}} (charter approval is separate
from the candidate hires below). Kernel and overlays selected; budgets,
checkpoints, and deliberate exclusions recorded in team.json.}}

## Methodology

Kernel {{kernel}} (in `methodology/kernel.md`), overlays {{overlays}}. Exposure
and combination rules are in `team.json` under `ensemble`.

## Running it

1. This package is **used separately** to do the underlying job — building it did
   not run it.
2. Runs land under `{{runs_dir}}`, one directory per run.
3. Parallel seats generate in isolation before combining; the synthesis preserves
   dissent (`references/synthesis-modes.md`). The user answers checkpoints; no
   script is run by hand.

## Declared exclusions

{{Which coverage axes this team deliberately does not cover — or "None: all axes
the assignment touches are covered."}}

## History

| Date | Change |
|---|---|
| {{created}} | Team created |
